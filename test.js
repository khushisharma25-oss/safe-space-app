const loginUser = async () => { try { const response = await fetch('http://localhost:5000/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ username: 'khushi_sharma_final', password: 'safepassword2026' }) }); const text = await response.text(); console.log('=== ASLI RESPONSE ==='); console.log('Status:', response.status); console.log(text.substring(0, 200)); } catch (error) { console.log(error); } }; loginUser();
